# DigitalAgenda v1 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un bot de Discord que a las 17:00 ofrece un menú y a las 21:00 registra por reacciones qué se tocó, y los viernes cuenta la semana. Lunes a viernes, sin juzgar.

**Architecture:** Tres capas con frontera clara: `mensajes.py` (texto puro, sin I/O), `store.py` (SQLite, sin Discord) y `bot.py` (Discord + scheduler, sin lógica). Las dos primeras se prueban con `pytest` sin token ni red; la tercera es pegamento fino y se prueba a mano en un canal de pruebas.

**Tech Stack:** Python 3.12, `discord.py` 2.x, `APScheduler` 3.x, `sqlite3` (stdlib), `uv`, `pytest`, `systemd`.

**Spec:** `docs/spec-v1.md`

## Global Constraints

- Python `>=3.12` (lo fija `uv`, no la instalación del sistema).
- Zona horaria `Europe/Madrid` en todos los cron y en el cálculo de "hoy".
- Solo días `mon-fri`. Fin de semana: el bot no emite nada.
- Categorías fijas: `1 paseo · 2 casa · 3 dibujar · 4 proyecto` (la 4 solo en captura).
- Ningún mensaje repite, insiste, compara semanas ni califica. Los textos son exactamente los de la spec.
- Config solo por variables de entorno: `DISCORD_TOKEN`, `CHANNEL_ID`, `DB_PATH` (opcional, default `agenda.db`).
- Commits pequeños y frecuentes; cada tarea termina en verde y commiteada.

### Analogía de fábrica para orientarse

`bot.py` es el inserter: coge del reloj (scheduler) y mete en Discord, y coge de Discord (reacciones) y mete en el cofre. `store.py` es el cofre. `mensajes.py` es la receta de ensamblado: entra fecha/conteos, sale texto. Los inserters no piensan; las recetas no tocan cintas.

---

## Estructura de archivos

```
pyproject.toml              deps y config de pytest
.env.example                variables que necesita el bot
.gitignore
src/agenda/__init__.py
src/agenda/categorias.py    tabla única de categorías y emojis (Task 2)
src/agenda/mensajes.py      construye textos: menu, captura, resumen (Task 3)
src/agenda/store.py         SQLite: mensajes enviados y capturas (Task 4)
src/agenda/semana.py        fechas: hoy en Madrid, lunes/viernes de la semana (Task 5)
src/agenda/bot.py           cliente Discord + scheduler + handlers (Task 6)
src/agenda/__main__.py      `python -m agenda` (Task 6)
tests/test_mensajes.py
tests/test_store.py
tests/test_semana.py
deploy/agenda.service       unidad systemd (Task 7)
README.md                   cómo crear el bot, canal, desplegar (Task 7)
```

---

### Task 1: Esqueleto del proyecto con uv

**Por qué:** `uv` fija Python y dependencias en un `uv.lock`, así el VPS instala exactamente lo mismo que tu PC.

**Files:**
- Create: `pyproject.toml`, `.gitignore`, `.env.example`, `src/agenda/__init__.py`, `tests/__init__.py`

- [ ] **Step 1: Crear los archivos**

`uv init --lib --name agenda --python 3.12 .` genera una base; si se queja del directorio no vacío, crea los archivos a mano con el contenido de los pasos siguientes. `src/agenda/__init__.py` y `tests/__init__.py` vacíos.

- [ ] **Step 2: Dejar `pyproject.toml` así**

```toml
[project]
name = "agenda"
version = "0.1.0"
description = "DigitalAgenda v1: bot de Discord que ofrece y registra"
requires-python = ">=3.12"
dependencies = [
    "discord.py>=2.4",
    "apscheduler>=3.10,<4",
]

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agenda"]

[tool.pytest.ini_options]
testpaths = ["tests"]
```

- [ ] **Step 3: `.gitignore` y `.env.example`**

`.gitignore`:
```
.venv/
__pycache__/
*.db
.env
```

`.env.example`:
```
DISCORD_TOKEN=pega-aqui-el-token-del-bot
CHANNEL_ID=123456789012345678
DB_PATH=agenda.db
```

- [ ] **Step 4: Instalar y comprobar que pytest arranca**

```bash
uv sync
uv run pytest
```
Expected: `no tests ran` (sin errores de import).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore: esqueleto uv con discord.py y apscheduler"
```

---

### Task 2: Categorías (la única tabla de verdad)

**Por qué:** número, clave, etiqueta y emoji viven en un solo sitio. Si mañana cambias "casa" por "orden", se cambia aquí y nada más.

**Files:**
- Create: `src/agenda/categorias.py`
- Test: `tests/test_mensajes.py` (se empieza aquí, se amplía en Task 3)

**Interfaces:**
- Produces: `CATEGORIAS: list[Categoria]`, `Categoria(numero:int, clave:str, etiqueta:str, emoji:str, solo_captura:bool)`, `por_emoji(emoji:str) -> Categoria | None`, `EMOJIS_MENU: list[str]`, `EMOJIS_CAPTURA: list[str]`.

- [ ] **Step 1: Test que falla**

```python
# tests/test_mensajes.py
from agenda.categorias import CATEGORIAS, por_emoji, EMOJIS_MENU, EMOJIS_CAPTURA

def test_categorias_fijas():
    assert [c.clave for c in CATEGORIAS] == ["paseo", "casa", "dibujar", "proyecto"]

def test_proyecto_solo_en_captura():
    assert len(EMOJIS_MENU) == 3
    assert len(EMOJIS_CAPTURA) == 4

def test_por_emoji():
    assert por_emoji("1️⃣").clave == "paseo"
    assert por_emoji("🍕") is None
```

- [ ] **Step 2: Ejecutar y ver que falla**

`uv run pytest tests/test_mensajes.py -v` → FAIL `ModuleNotFoundError`.

- [ ] **Step 3: Implementar**

```python
# src/agenda/categorias.py
from dataclasses import dataclass

# Los "keycap" de Discord son el dígito + U+FE0F + U+20E3.
def _keycap(n: int) -> str:
    return f"{n}️⃣"

@dataclass(frozen=True)
class Categoria:
    numero: int
    clave: str
    etiqueta: str
    emoji: str
    solo_captura: bool = False

CATEGORIAS: list[Categoria] = [
    Categoria(1, "paseo", "paseo", _keycap(1)),
    Categoria(2, "casa", "casa", _keycap(2)),
    Categoria(3, "dibujar", "dibujar", _keycap(3)),
    Categoria(4, "proyecto", "solo proyecto", _keycap(4), solo_captura=True),
]

EMOJIS_MENU = [c.emoji for c in CATEGORIAS if not c.solo_captura]
EMOJIS_CAPTURA = [c.emoji for c in CATEGORIAS]

def por_emoji(emoji: str) -> Categoria | None:
    for c in CATEGORIAS:
        if c.emoji == emoji:
            return c
    return None
```

- [ ] **Step 4: Verde** — `uv run pytest tests/test_mensajes.py -v` → 3 PASS.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: categorías fijas con emojis keycap"`

---

### Task 3: Textos de los mensajes

**Por qué:** funciones puras `fecha/conteos → str`. Sin Discord, sin base de datos. Aquí es donde vive el invariante "no castiga": los textos son literales y se prueban.

**Files:**
- Create: `src/agenda/mensajes.py`
- Modify: `tests/test_mensajes.py`

**Interfaces:**
- Consumes: `CATEGORIAS` de Task 2.
- Produces: `menu() -> str`, `captura() -> str`, `resumen(lunes: date, viernes: date, conteos: dict[str,int]) -> str`.

- [ ] **Step 1: Tests que fallan (añadir al final de `tests/test_mensajes.py`)**

```python
from datetime import date
from agenda.mensajes import menu, captura, resumen

def test_menu():
    assert menu() == "1️⃣ paseo · 2️⃣ casa · 3️⃣ dibujar · resto: proyecto"

def test_captura():
    assert captura() == "¿Qué tocaste hoy? 1️⃣ paseo · 2️⃣ casa · 3️⃣ dibujar · 4️⃣ solo proyecto"

def test_resumen_formato():
    txt = resumen(date(2026, 8, 24), date(2026, 8, 28),
                  {"paseo": 2, "casa": 1, "dibujar": 3, "proyecto": 1})
    assert txt == "Semana 24–28 ago\npaseo 2 · casa 1 · dibujar 3 · solo proyecto 1"

def test_resumen_sin_datos_no_juzga():
    txt = resumen(date(2026, 8, 24), date(2026, 8, 28), {})
    assert txt == "Semana 24–28 ago\npaseo 0 · casa 0 · dibujar 0 · solo proyecto 0"
```

- [ ] **Step 2: Ver que falla** — `uv run pytest tests/test_mensajes.py -v` → FAIL `ImportError`.

- [ ] **Step 3: Implementar**

```python
# src/agenda/mensajes.py
from datetime import date
from .categorias import CATEGORIAS

_MESES = ["ene", "feb", "mar", "abr", "may", "jun",
          "jul", "ago", "sep", "oct", "nov", "dic"]

def _linea(cats) -> str:
    return " · ".join(f"{c.emoji} {c.etiqueta}" for c in cats)

def menu() -> str:
    cats = [c for c in CATEGORIAS if not c.solo_captura]
    return f"{_linea(cats)} · resto: proyecto"

def captura() -> str:
    return f"¿Qué tocaste hoy? {_linea(CATEGORIAS)}"

def resumen(lunes: date, viernes: date, conteos: dict[str, int]) -> str:
    # Sin comparar, sin objetivo, sin flechas: solo contar.
    cabecera = f"Semana {lunes.day}–{viernes.day} {_MESES[viernes.month - 1]}"
    partes = [f"{c.etiqueta} {conteos.get(c.clave, 0)}" for c in CATEGORIAS]
    return f"{cabecera}\n{' · '.join(partes)}"
```

- [ ] **Step 4: Verde** — `uv run pytest -v` → todo PASS.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: textos de menú, captura y resumen"`

---

### Task 4: Store (SQLite)

**Por qué:** el bot se reiniciará (deploy, VPS). Guardamos qué `message_id` fue la captura de cada día para que una reacción que llegue tras un reinicio siga sabiendo a qué día pertenece. Las capturas son filas `(fecha, categoria)`: reaccionar añade, quitar la reacción borra.

**Files:**
- Create: `src/agenda/store.py`
- Test: `tests/test_store.py`

**Interfaces:**
- Produces: clase `Store(path: str)` con:
  - `guardar_mensaje(fecha: date, tipo: str, message_id: int) -> None`
  - `fecha_de_captura(message_id: int) -> date | None`
  - `registrar(fecha: date, categoria: str) -> None` (idempotente)
  - `quitar(fecha: date, categoria: str) -> None`
  - `conteos(desde: date, hasta: date) -> dict[str, int]` (días distintos por categoría, ambos inclusive)

- [ ] **Step 1: Tests que fallan**

```python
# tests/test_store.py
from datetime import date
import pytest
from agenda.store import Store

@pytest.fixture
def store(tmp_path):
    return Store(str(tmp_path / "t.db"))

def test_mensaje_de_captura(store):
    store.guardar_mensaje(date(2026, 8, 24), "captura", 111)
    store.guardar_mensaje(date(2026, 8, 24), "menu", 222)
    assert store.fecha_de_captura(111) == date(2026, 8, 24)
    assert store.fecha_de_captura(222) is None   # el menú no es captura
    assert store.fecha_de_captura(999) is None

def test_registrar_es_idempotente(store):
    d = date(2026, 8, 24)
    store.registrar(d, "paseo")
    store.registrar(d, "paseo")
    assert store.conteos(d, d) == {"paseo": 1}

def test_quitar(store):
    d = date(2026, 8, 24)
    store.registrar(d, "paseo")
    store.quitar(d, "paseo")
    assert store.conteos(d, d) == {}

def test_conteos_cuenta_dias_no_reacciones(store):
    store.registrar(date(2026, 8, 24), "dibujar")
    store.registrar(date(2026, 8, 25), "dibujar")
    store.registrar(date(2026, 8, 25), "casa")
    store.registrar(date(2026, 8, 31), "dibujar")   # fuera de rango
    assert store.conteos(date(2026, 8, 24), date(2026, 8, 28)) == {"dibujar": 2, "casa": 1}
```

- [ ] **Step 2: Ver que falla** — `uv run pytest tests/test_store.py -v` → FAIL.

- [ ] **Step 3: Implementar**

```python
# src/agenda/store.py
import sqlite3
from datetime import date

_SCHEMA = """
CREATE TABLE IF NOT EXISTS mensajes (
    fecha TEXT NOT NULL,
    tipo TEXT NOT NULL,
    message_id INTEGER PRIMARY KEY
);
CREATE TABLE IF NOT EXISTS capturas (
    fecha TEXT NOT NULL,
    categoria TEXT NOT NULL,
    PRIMARY KEY (fecha, categoria)
);
"""

class Store:
    def __init__(self, path: str):
        self._db = sqlite3.connect(path)
        self._db.executescript(_SCHEMA)

    def guardar_mensaje(self, fecha: date, tipo: str, message_id: int) -> None:
        with self._db:
            self._db.execute(
                "INSERT OR REPLACE INTO mensajes VALUES (?, ?, ?)",
                (fecha.isoformat(), tipo, message_id))

    def fecha_de_captura(self, message_id: int) -> date | None:
        row = self._db.execute(
            "SELECT fecha FROM mensajes WHERE message_id = ? AND tipo = 'captura'",
            (message_id,)).fetchone()
        return date.fromisoformat(row[0]) if row else None

    def registrar(self, fecha: date, categoria: str) -> None:
        with self._db:
            self._db.execute(
                "INSERT OR IGNORE INTO capturas VALUES (?, ?)",
                (fecha.isoformat(), categoria))

    def quitar(self, fecha: date, categoria: str) -> None:
        with self._db:
            self._db.execute(
                "DELETE FROM capturas WHERE fecha = ? AND categoria = ?",
                (fecha.isoformat(), categoria))

    def conteos(self, desde: date, hasta: date) -> dict[str, int]:
        rows = self._db.execute(
            "SELECT categoria, COUNT(DISTINCT fecha) FROM capturas "
            "WHERE fecha BETWEEN ? AND ? GROUP BY categoria",
            (desde.isoformat(), hasta.isoformat())).fetchall()
        return {cat: n for cat, n in rows}
```

- [ ] **Step 4: Verde** — `uv run pytest -v`.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: store sqlite de mensajes y capturas"`

---

### Task 5: Fechas de la semana

**Por qué:** "hoy" tiene que ser hoy en Madrid, no en UTC del VPS (a las 23:30 UTC ya es mañana en Madrid). Y el resumen necesita lunes y viernes de la semana en curso.

**Files:**
- Create: `src/agenda/semana.py`
- Test: `tests/test_semana.py`

**Interfaces:**
- Produces: `TZ = ZoneInfo("Europe/Madrid")`, `hoy() -> date`, `lunes_viernes(d: date) -> tuple[date, date]`.

- [ ] **Step 1: Tests que fallan**

```python
# tests/test_semana.py
from datetime import date
from agenda.semana import lunes_viernes

def test_lunes_viernes_desde_miercoles():
    assert lunes_viernes(date(2026, 8, 26)) == (date(2026, 8, 24), date(2026, 8, 28))

def test_lunes_viernes_desde_viernes():
    assert lunes_viernes(date(2026, 8, 28)) == (date(2026, 8, 24), date(2026, 8, 28))
```

- [ ] **Step 2: Ver que falla** — `uv run pytest tests/test_semana.py -v` → FAIL.

- [ ] **Step 3: Implementar**

```python
# src/agenda/semana.py
from datetime import date, datetime, timedelta
from zoneinfo import ZoneInfo

TZ = ZoneInfo("Europe/Madrid")

def hoy() -> date:
    return datetime.now(TZ).date()

def lunes_viernes(d: date) -> tuple[date, date]:
    lunes = d - timedelta(days=d.weekday())
    return lunes, lunes + timedelta(days=4)
```

- [ ] **Step 4: Verde** — `uv run pytest -v`.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat: fechas de la semana en Europe/Madrid"`

---

### Task 6: El bot (Discord + scheduler)

**Por qué:** es el inserter. No tiene lógica: llama a `mensajes` para el texto, a `store` para guardar, y al reloj para saber cuándo. Se usa `on_raw_reaction_add/remove` (y no `on_reaction_add`) porque los eventos *raw* llegan aunque el mensaje sea de antes del último reinicio.

**Files:**
- Create: `src/agenda/bot.py`, `src/agenda/__main__.py`

**Interfaces:**
- Consumes: `menu()`, `captura()`, `resumen()`, `Store`, `hoy()`, `lunes_viernes()`, `TZ`, `EMOJIS_MENU`, `EMOJIS_CAPTURA`, `por_emoji()`.

- [ ] **Step 1: Escribir `bot.py`**

```python
# src/agenda/bot.py
import logging
import os

import discord
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from apscheduler.triggers.cron import CronTrigger

from . import mensajes
from .categorias import EMOJIS_CAPTURA, EMOJIS_MENU, por_emoji
from .semana import TZ, hoy, lunes_viernes
from .store import Store

log = logging.getLogger("agenda")


class Agenda(discord.Client):
    def __init__(self, channel_id: int, store: Store):
        intents = discord.Intents.default()   # incluye guild_reactions; no hace falta message_content
        super().__init__(intents=intents)
        self.channel_id = channel_id
        self.store = store
        self.scheduler = AsyncIOScheduler(timezone=TZ)

    async def setup_hook(self) -> None:
        # Se ejecuta una vez, antes de conectar. Aquí se arma el reloj.
        self.scheduler.add_job(self.enviar_menu,
                               CronTrigger(day_of_week="mon-fri", hour=17, minute=0))
        self.scheduler.add_job(self.enviar_captura,
                               CronTrigger(day_of_week="mon-fri", hour=21, minute=0))
        self.scheduler.start()

    async def on_ready(self) -> None:
        log.info("conectado como %s", self.user)

    # --- emisión -------------------------------------------------------

    async def _canal(self) -> discord.TextChannel:
        return self.get_channel(self.channel_id) or await self.fetch_channel(self.channel_id)

    async def _enviar(self, tipo: str, texto: str, emojis: list[str]) -> discord.Message:
        canal = await self._canal()
        msg = await canal.send(texto)
        for e in emojis:              # pre-poner las reacciones = respuesta de un tap
            await msg.add_reaction(e)
        self.store.guardar_mensaje(hoy(), tipo, msg.id)
        return msg

    async def enviar_menu(self) -> None:
        await self._enviar("menu", mensajes.menu(), EMOJIS_MENU)

    async def enviar_captura(self) -> None:
        await self._enviar("captura", mensajes.captura(), EMOJIS_CAPTURA)
        if hoy().weekday() == 4:      # viernes: el resumen va pegado a la captura
            lunes, viernes = lunes_viernes(hoy())
            conteos = self.store.conteos(lunes, viernes)
            canal = await self._canal()
            await canal.send(mensajes.resumen(lunes, viernes, conteos))

    # --- captura por reacciones ---------------------------------------

    def _categoria_de(self, payload: discord.RawReactionActionEvent):
        if payload.user_id == self.user.id:          # nuestras propias pre-reacciones
            return None
        fecha = self.store.fecha_de_captura(payload.message_id)
        if fecha is None:                            # no es una captura (p.ej. es el menú)
            return None
        cat = por_emoji(str(payload.emoji))
        return (fecha, cat.clave) if cat else None

    async def on_raw_reaction_add(self, payload: discord.RawReactionActionEvent) -> None:
        if hit := self._categoria_de(payload):
            self.store.registrar(*hit)

    async def on_raw_reaction_remove(self, payload: discord.RawReactionActionEvent) -> None:
        if hit := self._categoria_de(payload):
            self.store.quitar(*hit)


def main() -> None:
    logging.basicConfig(level=logging.INFO)
    token = os.environ["DISCORD_TOKEN"]
    channel_id = int(os.environ["CHANNEL_ID"])
    store = Store(os.environ.get("DB_PATH", "agenda.db"))
    Agenda(channel_id, store).run(token, log_handler=None)
```

```python
# src/agenda/__main__.py
from .bot import main

main()
```

- [ ] **Step 2: Prueba manual en un canal de pruebas** (sin esperar a las 17:00)

Crear un `.env` real (ver README de Task 7). Cambiar provisionalmente `hour=17, minute=0` y `hour=21, minute=0` por la hora actual + 2 y + 4 minutos, y ejecutar:

```bash
uv run --env-file .env python -m agenda
```

Comprobar: llega el menú con 3 reacciones puestas; pulsar una reacción del menú **no** crea filas (es oferta); en la captura, pulsar 1️⃣ y 4️⃣ crea dos filas; quitar 1️⃣ la borra. Ver la BD:

```bash
uv run python -c "import sqlite3;print(sqlite3.connect('agenda.db').execute('select * from capturas').fetchall())"
```

Restaurar las horas reales antes del commit.

- [ ] **Step 3: Tests siguen verdes** — `uv run pytest`.

- [ ] **Step 4: Commit** — `git add -A && git commit -m "feat: bot de Discord con menú 17:00, captura 21:00 y resumen viernes"`

---

### Task 7: Despliegue en el VPS y README

**Por qué:** que sobreviva a reinicios del VPS y a que te olvides de él.

**Files:**
- Create: `deploy/agenda.service`, `README.md`

- [ ] **Step 1: `deploy/agenda.service`**

```ini
[Unit]
Description=DigitalAgenda bot
After=network-online.target

[Service]
User=agenda
WorkingDirectory=/opt/agenda
EnvironmentFile=/opt/agenda/.env
ExecStart=/opt/agenda/.venv/bin/python -m agenda
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 2: `README.md`**

```markdown
# DigitalAgenda v1

Bot de Discord que ofrece (17:00) y registra (21:00), lunes a viernes. Spec: `docs/spec-v1.md`.

## Crear el bot en Discord
1. https://discord.com/developers/applications → New Application → Bot → Reset Token → copiar a `.env`.
2. OAuth2 → URL Generator: scope `bot`; permisos `View Channel`, `Send Messages`, `Add Reactions`, `Read Message History`. Abrir la URL e invitarlo a tu servidor.
3. Crear canal privado `#agenda`; dar acceso al bot. Modo desarrollador en Discord → clic derecho en el canal → Copy Channel ID → `CHANNEL_ID` en `.env`.

## Local
    cp .env.example .env   # rellenar
    uv sync
    uv run pytest
    uv run --env-file .env python -m agenda

## VPS
    sudo useradd -r -m -d /opt/agenda agenda
    sudo -u agenda git clone <repo> /opt/agenda
    cd /opt/agenda && sudo -u agenda uv sync --no-dev
    sudo -u agenda cp .env.example .env && sudo -u agenda nano .env
    sudo cp deploy/agenda.service /etc/systemd/system/
    sudo systemctl daemon-reload && sudo systemctl enable --now agenda
    journalctl -u agenda -f

Actualizar: `git pull && uv sync --no-dev && sudo systemctl restart agenda`.
```

- [ ] **Step 3: Desplegar y verificar** — `journalctl -u agenda -f` muestra `conectado como <bot>`. Dejar correr; el lunes a las 17:00 debe llegar el primer menú.

- [ ] **Step 4: Commit** — `git add -A && git commit -m "chore: unidad systemd y README de despliegue"`

---

## Self-review

- **Cobertura de spec:** menú 17:00 ✔ (T6) · captura 21:00 multi-reacción ✔ (T4, T6) · resumen viernes formato exacto ✔ (T3, T6) · fin de semana silencio ✔ (`mon-fri`) · categorías fijas ✔ (T2) · reacciones al menú no cuentan ✔ (`fecha_de_captura` solo tipo captura) · texto de Corco ignorado ✔ (sin `on_message`) · env vars ✔ · systemd ✔ · TZ Madrid ✔ (T5, scheduler).
- **Sin placeholders.**
- **Firmas consistentes:** `resumen(lunes, viernes, conteos)`, `Store.conteos(desde, hasta)`, `fecha_de_captura(message_id)`, `por_emoji(str)` usadas igual en T3/T4/T6.
